#!/usr/bin/env bash
set -euo pipefail
ROOT="$(cd "$(dirname "$0")/.." && pwd)"
SRC="$ROOT/resources/styles"
DEST="$ROOT/public/assets/css"
mkdir -p "$DEST"
if [[ -d "$SRC" ]]; then
  cp -a "$SRC/." "$DEST/"
  echo "Styles copiés : $SRC → $DEST"
else
  echo "Pas de $SRC, CSS servi depuis $DEST"
fi
